import { en } from '../i18n/dictionaries';

/** @deprecated Use `useLanguage()` hook instead */
export const text = {
    chooseRaceFromList: en.createFromList,
    back: en.back,
    createNewRace: en.createNewRace,
    InsertRaceName: en.insertRaceName,
    edit: en.edit,
    hours: en.hours,
    minutes: en.minutes,
    seconds: en.seconds,
    setTime: en.setTime,
};
